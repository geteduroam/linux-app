[Unit]
Description=This service runs a getgovroam command that checks if the govroam \
connection profile is about to expire and if so it shows a notification

[Service]
Type=oneshot
ExecStart=/usr/bin/getgovroam-notifcheck
